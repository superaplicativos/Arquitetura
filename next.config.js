/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/Arquitetura',
  assetPrefix: '/Arquitetura/',
}

module.exports = nextConfig

// Made with Bob
